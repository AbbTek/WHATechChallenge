@{
    ViewBag.Title = "Whilliams Hill Code Test";
}

<div ng-controller="betsController">
    <h2>Upload settled bets</h2>
    <input type="file" nv-file-select uploader="uploader" />
    <br />
    <ul>
        <li ng-repeat="item in uploader.queue">
            Name: <span ng-bind="item.file.name"></span><br />
            <button ng-click="item.upload()">upload</button>
        </li>
    </ul>
    @*<button type="button" class="btn btn-xs" ng-click="test()">
        <i class="glyphicon glyphicon-remove" tooltip="Test" tooltip-placement="right"></i>
    </button>*@
</div>